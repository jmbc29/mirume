{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "review",
  "description": "Capability for the review window",
  "windows": ["review"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
